深度搜索, 逐步的, 每步搜索一个查询, 逐步串行地从互联网中搜索收集所需的多个维度信息:
1. 搜索主流代码风格指南（PEP8、Google Java、Airbnb JS、Google JS）中关于命名大小写(case)规范的层次结构
2. 搜索命名大小写作为代码视觉外观评估维度的设计方法
3. 搜索多语言项目中命名规范冲突与解决策略

当上下文未提供必要属性所需的数据信息时，使用显式标记默认占位填充。禁止自行生成虚构数据。

附加格式要求:
1. 总-分结构：先给审查结论摘要，再逐点分析
2. 聚焦代码视觉外观——不同命名大小写在屏幕上看起来有什么不同
3. 如有问题，给出完整替换 descriptions 数组（6个字符串，对应0-5级）

已知上下文:
- 项目为代码风格调优器（50维度/10分组/0-5量表）
- 仅关心代码视觉外观（看起来长什么样）
- 量表统一 0-5 离散档，0=最宽松，5=最严格

任务:
审查维度"命名大小写规范 (Naming Casing Convention)"的0-5级描述:

0: 无任何命名约定，允许随意混合不同大小写与分隔样式
1: 同一代码库内要求命名风格一致（全部标识符采用同一类分隔方式，如全小驼峰或全下划线），但不限定具体样式
2: 按标识符类型区分命名风格，并要求类型间风格有明显视觉区别，但不限定使用哪一种具体样式组合
3: 标识符命名风格必须符合该语言社区的主流惯例，允许存在少数有文档说明的例外
4: 严格遵循指定的语言社区官方风格指南（如 PEP 8、Google Java Style Guide），无例外
5: 命名规范通过静态分析工具自动强制执行，任何违规均被构建或提交阻断

审查：
1. 0-5是否形成严格度单向递增（约束范围逐步扩大）？
2. 3级与4级的差异是否足够清晰（主流惯例 vs 官方指南）？
3. 描述是否聚焦代码视觉外观？是否存在风格偏见？
4. 有问题给出完整替换 descriptions 数组。
