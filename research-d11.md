深度搜索, 逐步的, 每步搜索一个查询, 逐步串行地从互联网中搜索收集所需的多个维度信息:
1. 搜索主流代码风格指南（Google、Airbnb、PEP8等）中关于内联注释（inline comments）的规范
2. 搜索内联注释密度对代码视觉外观和可读性影响的实证研究
3. 搜索内联注释作为代码风格光谱维度的设计方法

当上下文未提供必要属性所需的数据信息时，使用显式标记默认占位填充。禁止自行生成虚构数据。

附加格式要求:
1. 总-分结构：先给审查结论摘要，再逐点分析
2. 聚焦代码视觉外观——不同内联注释密度在屏幕上看起来有什么不同
3. 如有问题，给出完整替换 descriptions 数组（6个字符串，对应0-5级）

已知上下文:
- 项目为代码风格调优器（50维度/10分组/0-5量表）
- 仅关心代码视觉外观（看起来长什么样）
- 量表统一 0-5 离散档

任务:
审查维度"内联注释风格 (Inline Comment Style)"的0-5级描述:

0: 屏幕上只有纯粹的代码，没有任何注释符号或自然语言，视觉上干净无干扰
1: 整个文件中零星出现极少数行末注释，像代码海洋中偶然飘过的浮叶，几乎不可察觉
2: 在代码结构复杂的局部区域，会看到几处行尾注释集中出现，成为该区域的视觉标注点
3: 代码被规律放置的独占行注释分割成明确的步骤块，注释行形成水平分隔线般的作用
4: 注释行高频出现，与代码行交替排列，几乎每屏都有多行自然语言，产生叙述流的视觉效果
5: 注释文字占据屏幕主体，代码仿佛穿插其中的附注，页面看起来像一篇密集的编码日志

审查：
1. 0-5是否形成合理的视觉密度递增光谱？
2. 描述是否如实反映代码屏幕外观？是否使用了恰当的视觉隐喻（浮叶、分隔线、叙述流等）？
3. 是否存在过度修辞导致描述失真？
4. 如有问题，给出完整替换 descriptions 数组。
