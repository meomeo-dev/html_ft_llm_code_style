深度搜索, 逐步的, 每步搜索一个查询, 逐步串行地从互联网中搜索收集所需的多个维度信息:
1. 搜索主流代码风格指南（Google Style Guide、Airbnb JS Style Guide、PEP 8等）中关于缩进一致性的规范
2. 搜索代码审查中"缩进风格一致性"作为评估维度的常见做法
3. 搜索缩进风格光谱设计的反模式

当上下文未提供必要属性所需的数据信息时，使用显式标记默认占位填充。禁止自行生成虚构数据。

附加格式要求:
1. 总-分结构：先给审查结论摘要，再逐点分析
2. 聚焦代码视觉外观——不同缩进风格在屏幕上看起来有什么不同
3. 如有问题，给出完整替换 descriptions 数组（6个字符串，对应0-5级）

已知上下文:
- 项目为代码风格调优器（50维度/10分组/0-5量表），仅关心代码视觉外观
- 该维度之前版本已从混合风格选择的错误设计修正为纯一致性光谱

任务:
审查维度"缩进风格一致性 (Indentation Style Consistency)"的当前0-5级描述:

0: 不强制缩进，允许 Tab 与空格混用，甚至同一文件内混合
1: 单个文件内缩进字符一致（全 Tab 或全空格），但不同文件间可能不同
2: 项目内统一缩进字符（Tab 或空格），但缩进宽度未强制（如 Tab 宽度可因人而异，或空格数未统一）
3: 项目内统一缩进字符且固定宽度（Tab 宽度由 .editorconfig 锁定，或明确为 2/4/8 空格），保证任何环境下视觉宽度一致
4: 通过 linter/格式化器检查缩进一致性，违规发出警告或阻止提交
5: 通过工具自动修复所有缩进违规，无需人工干预，零容忍不一致

审查：
1. 0-5是否形成严格的视觉一致性递增光谱？
2. 是否存在风格偏见（如暗示某种缩进方式更优）？
3. 4级和5级（linter检查 vs 自动修复）视觉结果相同，区分是否有意义？
