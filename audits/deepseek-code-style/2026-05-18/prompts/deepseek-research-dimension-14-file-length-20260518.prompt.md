深度搜索, 逐步的, 每步搜索一个查询, 逐步串行地从互联网中搜索收集所需的多个维度信息:
1. 搜索主流代码风格指南如何描述源文件长度、模块大小、单文件行数或拆分文件建议。
2. 搜索主流 linters、静态分析工具或度量工具对 max-lines、file length、class/file size 的规则。
3. 搜索代码阅读、代码审查或 IDE 资料中，关于滚动条、minimap、文件长度与视觉导航的观点。
4. 搜索反例：哪些场景允许较长文件，并说明其边界，不要把例外误写成外观量表的常态。
5. 汇总这些资料对“代码长什么样”的启发，不要转向技术能力、语言特性、复杂度或架构优劣。
6. 评估下面 0-5 档文案是否形成单调、清晰、互斥的视觉量表。
7. 给出仅限本维度的修改建议；如果无需修改，明确说明。

我们需要同时探索多个方向, 针对研究中的缺口进行补充搜索。

当上下文未提供必要属性所需的数据信息时：
- 若该信息**无法通过上下文推断或合理派生**，则使用显式标记默认占位填充（templated placeholder, UPPER_CASE）提示用户补充
- 若该信息**可通过上下文中的其他信息推断得出**，则可直接使用推断值
- **禁止**自行生成"看似真实"但实际未提供的具体数据（如虚构的ID、时间戳、用户名等）

附加格式要求:

1. 用摘要或关键词概括那个连续段落的核心内容，并放在问题之前, 总-分的写作方法。
2. 在连续段落的开头或结尾加入信号词, 信号词标记方式与回复格式保持一致, 比如markdown时请使用 `**` 加粗标记，并保持格式统一。信号词中英文对照, 术语英文放到括号内。只需要使用标记方式标记信号词即可, 无需出现“信号词”字眼，保持自然行文。
3. 自然地集中出现该段落内容主题独有的专有名词或术语，但不要过度堆砌，保持自然行文。
4. 使用任何能别出“章节边界”、并促使章节内部术语集中的标记方式分割长文档(默认为(markdown+mathjax+补充HTML片段(内联样式))作为回复格式, 否则按照用户指定格式回复), 使用明显的标题或分隔符标记出章节边界，使得每个章节内部的块更加同质。
5. 对于上下文要额外注意, 由于注意力机制, 召回上下文时会召回紧密衔接的内容，上下文可能无法清晰切割, 因此需要注意用户问题的严格边界。
6. 每次回复需要加上唯一ID(格式: 回复ID: <最最重要的那个关键词,驼峰写法>-<YYYYMMDD日期>-<\d{3}自增长>), 方便后序引用(引用格式: `[inner: 回复ID]`)。

数学公式使用 mathjax 表达, 特殊token表达放到 markdown 代码块中。

已知上下文:

- 审查对象是一个“AI 代码风格调优器”的单个维度文案。
- 用户关心“写出来的代码长什么样”，不关心技术难度、语言特性炫技、框架能力或独特语法。
- 本轮只审查第 14 维度：文件长度限制 (File Length)。
- 当前 0-5 档文案如下：
  - 0: 文件如同无尽的画卷，滚动条滑块极小，轻轻一拖就跳过数百行，无法感知文件全貌
  - 1: 文件明显超出几屏，连续快速滚动时内容持续涌现，minimap 呈现密集的色块条带
  - 2: 文件通常在两三屏以内，滚动一两次即到底，minimap 上能模糊分辨段落
  - 3: 文件约等于一屏到一屏半的内容，几乎不用滚动，打开瞬间结构一目了然
  - 4: 文件严格保持在一屏高度内，所有代码尽收眼底，像一页完整的笔记
  - 5: 文件极短，往往只比终端默认高度稍多，代码区域被大量空白包围，画面干净

任务:

请联网检索并审查该维度是否适合作为“代码外观”量表。重点判断：
- 名称是否准确表达单个源文件在屏幕中的视觉高度和滚动规模。
- 0-5 档是否单调递增，且每一档都能被肉眼观察到。
- 档位之间是否存在方向混淆：本量表分数越高是否应代表文件越短、更容易看到整体。
- 是否有不必要的误导，例如把模块组织、职责拆分、架构优劣或工程成熟度混入外观量表。
- 如需修改，只给出本维度 6 条替代文案；不要审查其他维度。
