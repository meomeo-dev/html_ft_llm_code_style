深度搜索, 逐步的, 每步搜索一个查询, 逐步串行地从互联网中搜索收集所需的多个维度信息:
1. 搜索主流代码风格指南中关于变量命名描述性的规范与建议
2. 搜索变量名称长度与代码可读性/视觉扫描效率的研究
3. 搜索命名描述性作为代码风格光谱维度的设计方法

附加格式要求:
1. 总-分结构：先给审查结论摘要，再逐点分析
2. 聚焦代码视觉外观——不同描述性级别的变量名在屏幕上看起来有什么不同
3. 如有问题，给出完整替换 descriptions 数组（6个字符串，0-5级）

已知上下文:
- 代码风格调优器，仅关心代码视觉外观，0-5量表

任务:
审查维度"变量命名描述性 (Variable Naming Descriptiveness)":

0: 单字母或占位符名（如 i, x, tmp），完全无描述性
1: 极简缩写，仅局部可读（如 cnt, idx, msg）
2: 单个完整单词（如 count, index, name），具备基本含义
3: 清晰的复合名称（如 userCount, isActive），意图明显且简短
4: 描述性短语，带必要限定词（如 numberOfActiveUsers, maxRetryCount）
5: 精准上下文命名，融入范围或单位，但保持简洁（如 activeUserCountInSession, elapsedTimeMs）

审查：0-5是否形成合理的描述性递增光谱？5级是否正确封顶（不过度冗长）？描述是否聚焦视觉外观？给结论。
